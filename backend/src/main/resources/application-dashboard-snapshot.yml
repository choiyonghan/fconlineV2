# GitHub Actions cron 배치 실행 전용 프로파일 — 매일 아침 메인 대시보드 스냅샷 생성.
# 사용: java -jar app.jar --spring.profiles.active=dashboard-snapshot
spring:
  main:
    web-application-type: none
    banner-mode: off
  datasource:
    hikari:
      maximum-pool-size: 3

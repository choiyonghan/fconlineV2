[versions]
java = "21"
# Spring Boot 3.5 라인은 2026-06-30 EOL(최종 3.5.16)이지만, 4.x는 이 프로젝트 작성 시점의
# 학습 데이터 이후 릴리스라 API 세부사항을 신뢰할 수 없어 3.x 계열을 그대로 유지하기로 결정함.
springBoot = "3.5.16"
springDependencyManagement = "1.1.7"
# 원본 QueryDSL(com.querydsl)은 아카이브되어 Jakarta/Spring Boot 3.x 환경에서는
# 커뮤니티 표준인 OpenFeign 포크를 사용한다.
querydsl = "6.11"
springdoc = "2.6.0"
lombok = "1.18.34"

[libraries]
querydsl-jpa = { module = "io.github.openfeign.querydsl:querydsl-jpa", version.ref = "querydsl" }
querydsl-apt = { module = "io.github.openfeign.querydsl:querydsl-apt", version.ref = "querydsl" }
springdoc-openapi-webmvc-ui = { module = "org.springdoc:springdoc-openapi-starter-webmvc-ui", version.ref = "springdoc" }
lombok = { module = "org.projectlombok:lombok", version.ref = "lombok" }

[plugins]
spring-boot = { id = "org.springframework.boot", version.ref = "springBoot" }
spring-dependency-management = { id = "io.spring.dependency-management", version.ref = "springDependencyManagement" }
